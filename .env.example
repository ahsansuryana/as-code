# MCP Server Configuration
PORT=3000

# Your publicly accessible URL (Cloudflare tunnel URL or domain)
# Example: https://abc123.trycloudflare.com
# Used for OAuth discovery and issuer metadata
PUBLIC_URL=https://your-tunnel-url.trycloudflare.com

# Bearer token for HTTP auth — generate with: npm run generate-token
# This prints a raw token (save it in a password manager, never in this file)
# and a BEARER_TOKEN_HASH line to paste below. Only the hash is stored on
# disk, so a leaked .env does not hand out a working credential.
BEARER_TOKEN_HASH=scrypt$replace-with-output-of-npm-run-generate-token

# Absolute path to the project root that the agent is allowed to access on host
# Local execution: F:/my-project
# Docker execution: Set HOST_PROJECT_PATH=F:/my-project (container will map it to /workspace)
PROJECT_ROOT=F:/my-project
HOST_PROJECT_PATH=F:/my-project

# Git HTTPS credentials (optional)
GIT_USER=your-github-username
GIT_PAT=github_pat_xxxxxxxxxxxxxxxxxxxx

# Optional Git commit identity (applied only to Git processes)
GIT_COMMIT_NAME=Your Name
GIT_COMMIT_EMAIL=you@example.com

# Permission mode: manual | scoped-auto | bypass
PERMISSION_MODE=manual

# Tool Profile: full | read-only | compat-readonly-all
MCP_TOOL_PROFILE=full

# Skip explicit permission prompts (default false)
MCP_SKIP_PERMISSIONS=false

# Watchdog timeout for background execution sessions in ms (default 30 mins)
SESSION_WATCHDOG_TIMEOUT_MS=1800000

# Command allowlist for exec_command / run_command (comma-separated executable names)
COMMAND_ALLOWLIST=npm,git,node,npx,python,python3,make,curl,docker

# Max timeout for blocking exec_command in ms (ceiling)
COMMAND_TIMEOUT_CEILING_MS=600000

# SQLite audit log path
AUDIT_DB_PATH=./data/audit.db

# Optional: web_fetch domain allowlist (comma-separated, empty = block all)
WEB_FETCH_ALLOWLIST=

# Bind address (use 127.0.0.1 for local host, or 0.0.0.0 for Docker container)
BIND_ADDRESS=127.0.0.1
